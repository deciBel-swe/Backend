spring.config.import=optional:file:.env[.properties]

#postGres
spring.application.name=DeciBel
spring.profiles.active=dev
server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5332/DeciBel
spring.datasource.username=DeciBel
spring.datasource.password=DeciBel1234
#updates tables to match on each run
spring.jpa.hibernate.ddl-auto=update
#hibernate & jpa settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
#base url for frontend
app.frontend-base-url=https://decibel.foo
# google oauth
app.google.redirect-uri=http://localhost:8081

# --- WEB ---
spring.security.oauth2.client.registration.google-web.provider=google
spring.security.oauth2.client.registration.google-web.client-id=122799926632-5200gd943j11fcg3g3f3t5b9jnn0buhj.apps.googleusercontent.com
spring.security.oauth2.client.registration.google-web.client-secret=${GOOGLE_WEB_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google-web.redirect-uri=localhost:8081

# --- DESKTOP ---
spring.security.oauth2.client.registration.google-desktop.provider=google
spring.security.oauth2.client.registration.google-desktop.client-id=122799926632-5200gd943j11fcg3g3f3t5b9jnn0buhj.apps.googleusercontent.com
spring.security.oauth2.client.registration.google-desktop.client-secret=${GOOGLE_DESKTOP_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google-desktop.redirect-uri=localhost:8081

# --- MOBILE ---
spring.security.oauth2.client.registration.google-mobile.provider=google
spring.security.oauth2.client.registration.google-mobile.client-id=122799926632-5200gd943j11fcg3g3f3t5b9jnn0buhj.apps.googleusercontent.com
spring.security.oauth2.client.registration.google-mobile.redirect-uri=localhost:8081
#--- STRIPE ---
stripe.api.key=${STRIPE_API_KEY:}
stripe.price.pro.id=${STRIPE_PRICE_PRO_ID:}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:}

#--- FireBase ---
firebase.service-account-path=firebase-service-account.json

#--- migrations ---
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

#mail configuration  to email reset password link
#decibel
app.cors.allowed-origins=*
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=decibel268@gmail.com
spring.mail.password=tiewhwzldqnxfhru
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
logging.level.org.springframework.web.socket=TRACE
logging.level.org.springframework.messaging=TRACE
server.tomcat.max-part-count=50
server.servlet.context-path=/api
# Forces TLS upgrade on connection ? no silent fallback to plain text
spring.mail.properties.mail.smtp.starttls.required=true
# Trusts Gmail's SMTP certificate explicitly ? fixes PKIX path building SSL error
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
# File upload and requests
# We will increase threshold so that max size can be handled and validated using business logic not here
spring.servlet.multipart.max-file-size=1000MB
spring.servlet.multipart.max-request-size=2500MB
#Microsoft azure
azure.storage.connection-string=${AZURE_STORAGE_CONNECTION_STRING}

#--- Captcha ---
google.recaptcha.enterprise.project-id=${RECAPTCHA_PROJECT_ID}
google.recaptcha.enterprise.api-key=${RECAPTCHA_API_KEY}
google.recaptcha.enterprise.site-key=${RECAPTCHA_SITE_KEY}
captcha.bypass-token=testing
email.bypass-domain=${BYPASS_EMAIL:}

azure.storage.blob-container-name=uploads

# Monitoring
management.endpoints.web.exposure.include=health,metrics,prometheus
management.endpoint.health.show-details=always
management.metrics.enable.management.health.status=true